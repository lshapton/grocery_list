<%= form_for(supply_variant) do |f| %>
  <% if supply_variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supply_variant.errors.count, "error") %> prohibited this supply_variant from being saved:</h2>

      <ul>
      <% supply_variant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
